import { Link } from "react-router-dom";

const NotFoundPage = () => {
  return (
    <section>
      <h1>NotFoundPage</h1>
      <Link to="/">Back to home</Link>
    </section>
  );
};

export default NotFoundPage;
